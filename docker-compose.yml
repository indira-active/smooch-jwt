version: '3'
services:
  smooch-jwt:
    build:
      context: .
      args:
        - CODECOV_TOKEN=$CODECOV_TOKEN
    image: smooch-jwt
    volumes:
      - .:/app
    ports:
      - '8080:8080'
    command: npm start